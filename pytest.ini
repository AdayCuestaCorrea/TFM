[pytest]
testpaths = tests
# rootdir on sys.path so `from src.ingestion...` resolves without an editable install.
pythonpath = .
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    slow: tests that build or train a TensorFlow model
